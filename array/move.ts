/**
 * Перемещает элемент в массиве с сохранением порядка
 *
 * @example
 *   move(0, 3, ['a', 'b', 'c', 'd']) => ['b', 'c', 'd', 'a']
 */

export function move<T>(a: number, b: number, list: T[]): T[] {
    //...
}
