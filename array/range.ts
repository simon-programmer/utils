/**
 * Создает массив цифр в заданном диапазоне и шагом
 *
 * @example
 *   range(0, 5) => [0, 1, 2, 3, 4]
 *   range(0, 20, 5) => [0, 5, 10, 15]
 *   range(0, -5, -1) => [0, -1, -2, -3]
 */

export function range(from: number, to: number, step = 1): number[] {
    //...
}
